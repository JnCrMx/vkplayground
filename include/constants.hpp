#include <cstdint>
#include <string>
#include <vulkan/vulkan.h>

namespace constants
{
	inline const std::string displayname = "VkPlayground";
	inline const std::string name = "vkplayground";

	inline const uint32_t version = 1;
}